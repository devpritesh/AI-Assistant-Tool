First checck Python install in the system
Then Check Python version by running command in cmd python --version 
version should be Python 3.11.9
if all ok then extract zip file 
open the extract folder AItool_app in cmd and past pip install -r requirements.txt command on cmd it will install all Dependencies require in the project.
after succesful install all Dependencies then past python app.py on first run it will take time to downlad a modle which is require.
if no error occur it will show following text .

//D:\Project\AItool_app>python app.py
 * Serving Flask app 'app'
 * Debug mode: on
WARNING: This is a development server. Do not use it in a production deployment. Use a production WSGI server instead.
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
 * Restarting with watchdog (windowsapi)
 * Debugger is active!
 * Debugger PIN: 784-899-993//

just copy http://127.0.0.1:5000 this network path and past it to the web browser it will run the web application on your browser 
select which tool you wont to use.
